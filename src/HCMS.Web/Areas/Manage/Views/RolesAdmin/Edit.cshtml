@model HCMS.Web.Areas.Manage.Models.RoleViewModel

@{
    var Phrases = (HCMS.Dynamics.Localization.Phrases)ViewBag.Phrases;
    ViewBag.Title = Phrases.Get("Role Managment | Edit Role");
}

<h1>
    @ViewBag.title
</h1>

@Html.Partial("Forms/ObjectForm",Model)

<div class="text-left">
    @Html.ActionLink(Phrases.Get("back To List"), "Index", null, new { @class = "btn btn-default" })
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
