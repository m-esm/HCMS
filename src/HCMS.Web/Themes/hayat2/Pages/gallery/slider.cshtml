@using HCMS.Dynamics.Data

@model IEnumerable<HCMS.Dynamics.Data.DRow>
@{
    var j = 0;
    var k = 0;

    var block = DDB.GetTable("cms", "blocks").DRows;

    var blockName = "";
    var model = Model.FirstOrDefault();
    if (model != null)
    {
        int blockId = Convert.ToInt32(model.GetValue("blockId").Value);
        blockName = block.First(a => a.RowID == blockId).GetValue("name").Value;
    }
}


  

    <div class="spinner">
        <img src="img/loader.gif">
    </div>
 <div class="gallery">

    <div class="gallery__view">
        <div class="gallery__thumbs-wrapper">
            <button class="scroll__btn left__btn">
                <img src="img/left-button.png" alt="left" width="25px" height="45px">
            </button>
            <button class="scroll__btn right__btn">
                <img src="img/right-button.png" alt="left" width="25px" height="45px">
            </button>

            <div class="gallery__thumbs">
                <!-- Rendering thumbs with jQuery-->
            </div>
        </div>
    </div>

</div>